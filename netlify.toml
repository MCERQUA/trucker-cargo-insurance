[build]
  command = "pnpm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

[[notifications]]
  event = "submission_created"
  type = "outgoing_webhook"
  url = "https://josh.jam-bot.com/social-api/api/leads/webhook/netlify?tenant=josh&site=trucker-cargo-insurance"
